[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ace-ego-0"
version = "0.2.0"
description = "ACE-Ego-0 inference, RoboCasa evaluation, and public Common23 SFT."
readme = "README.md"
requires-python = ">=3.10,<3.11"
license = {file = "LICENSE"}
dependencies = [
  "torch==2.6.0",
  "torchvision==0.21.0",
  "transformers==4.57.0",
  "accelerate==1.12.0",
  "diffusers==0.36.0",
  "omegaconf==2.3.0",
  "pyyaml>=6.0",
  "numpy==1.26.4",
  "pillow>=10",
  "scipy",
  "opencv-python==4.11.0.86",
  "matplotlib",
  "msgpack",
  "websockets",
  "gymnasium",
  "tyro==1.0.16",
  "av",
  "mujoco==3.2.6",
  "mink==0.0.5",
  "qpsolvers[quadprog]>=4.12.0",
  "yourdfpy",
  "pydantic==2.10.6",
]

[project.optional-dependencies]
dev = ["pre-commit", "ruff"]
train = [
  "deepspeed==0.16.9",
  "pyarrow==14.0.1",
]
wandb = ["wandb>=0.19,<1"]

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["ace_ego_0*", "evaluation*"]

[tool.ruff]
line-length = 121
target-version = "py310"

[tool.ruff.lint]
select = ["A", "B", "E", "F", "I", "RUF", "W"]
ignore = ["E501", "F722"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
